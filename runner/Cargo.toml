[package]
name = "autopilot"
version = "0.3.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
time = { version = "0.3", features = ["formatting", "macros", "local-offset"] }
libc = "0.2"

[profile.release]
panic = "abort"
